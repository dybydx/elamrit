---
import { cn } from '@/lib/utils';

const { class: className, ...rest } = Astro.props as { class?: string } & Record<string, unknown>;
---

<div data-slot="card-footer" class={cn('flex flex-wrap items-center gap-2', className)} {...rest}>
  <slot />
</div>
